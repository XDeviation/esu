[tool.poetry]
name = "server"
version = "0.1.0"
description = ""
authors = ["XDeviation <xth9681@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.8"
fastapi = "^0.115.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
